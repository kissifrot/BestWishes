{%- if purchasedGift.surprise -%}
{{ 'mail.alert_surprise_purchase.body_text'|trans({ '%siteName%': site_name, '%userName%': user.name, '%purchasedGiftName%': purchasedGift.name, '%buyerName%': buyer.name, '%listName%': purchasedGift.list.name, '%siteUrl%': home }) }}
{%- else -%}
{{ 'mail.alert_purchase.body_text'|trans({ '%siteName%': site_name, '%userName%': user.name, '%purchasedGiftName%': purchasedGift.name, '%buyerName%': buyer.name, '%listName%': purchasedGift.list.name, '%siteUrl%': home }) }}
{%- endif -%}
